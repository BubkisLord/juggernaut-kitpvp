# Run as the caster, positioned at the landing spot chosen by scatter_owner. Coordinates only, so
# the caster keeps looking wherever they were already looking instead of having their camera yanked.
tp @s ~ ~ ~
tag @s add pp_scattered
